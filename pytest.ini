[pytest]
asyncio_mode = auto
testpaths = tests
# Put the repo root on sys.path so `backend` imports resolve when pytest
# is invoked directly. Without this only `python -m pytest` works, since
# that form adds the working directory itself.
pythonpath = .
